{
  "name": "n24-data-deploy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "24n.cz — публичное хранилище новостей (articles.json) + CI-деплой на хостinг по FTPS",
  "scripts": {
    "index": "pagefind --site site/dist",
    "deploy": "node .github/scripts/deploy.mjs"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
